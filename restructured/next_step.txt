Add map screen with world and region views
  Add region name, borders, exploration %, current region with blinking @
Pass the visual data in single characters up to the window level, where it is concatenated
  just before sending to the UI!